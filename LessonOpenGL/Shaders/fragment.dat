#version 330 core

out vec4 FragColor;

uniform float time;
uniform float blend;

uniform sampler2D ourTexture1;
uniform sampler2D ourTexture2;

in vec4 vColor;
in vec2 TexCoord;
in vec4 Pos;

void main()
{
	//FragColor = mix(texture(ourTexture1,TexCoord),texture(ourTexture2,TexCoord),blend);
	FragColor = vec4(sin( Pos.x * time) / 2. + 0.5, cos(Pos.y) / 2. + 0.5, sin(time) / 2. + 0.5f, 1.f);//sin(Pos.y) / 2. + 0.5, 1.f);
}